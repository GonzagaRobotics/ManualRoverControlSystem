version: '2'

services:
    ros_echo:
      build: 
       context: ../..
       dockerfile: docker_scripts/for_jetson/jetsonDockerfile
      network_mode: "host"
      pid: "host"
      command: ros2 topic list
      restart: always
