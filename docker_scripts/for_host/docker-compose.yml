version: '2'

services:
    xbox_controller:
        build:
            context: ../../
            dockerfile: docker_scripts/for_host/hostDockerfile
        devices:
          - /dev/input/js0:/dev/input/js0
          - /dev/bus/usb/001/015:/dev/bus
        privileged: true
        network_mode: "host"
        pid: "host"
        command: ros2 launch autolaunch/launch/auto_launch.py
