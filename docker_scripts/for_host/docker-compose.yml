version: '2'

services:
    xbox_controller:
        build:
            context: ../
            dockerfile: docker_scripts/hostDockerfile
        command: ros2 launch autolaunch/launch/auto_launch.py
